{
    "version": "1.0",
    "name": "COtrip Dynamic Message Signs",
    "description": "Ingests dynamic message sign locations and messages from the Colorado COtrip system and posts them to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "COtrip Signs posts dynamic message sign locations to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls the Colorado COtrip system for updated message sign data on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(5 minutes)"
                }
            }
        }
    }
}
